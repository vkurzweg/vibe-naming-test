{
  "dependencies": {
    "@tanstack/react-query": "^5.85.0",
    "bootstrap": "^5.3.7",
    "express-session": "^1.18.2",
    "react-spring": "^10.0.1"
  },
 "scripts": {
 "heroku-postbuild": "cd client && npm install && npm run build && cp -r build ../server/",
  "start": "cd server && npm install && npm start"
}
}