{
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "heroku-postbuild": "cd namingops/client && npm install && npm run build && rm -rf ../server/build && cp -r build ../server/build",
    "start": "cd namingops/server && npm install && npm start"
  }
}